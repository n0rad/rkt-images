name: aci.awired.net/aci-arch-deluge

builder:
  dependencies:
    - aci.awired.net/aci-arch-yaourt-auto

aci:
  app:
    exec:
      - /usr/sbin/deluged
      - --loglevel=info
      - -d
      - -c /config

    mountPoints:
      - name: "config"
        path: "/config"
      - name: "downloads"
        path: "/downloads"
  dependencies:
    - aci.awired.net/aci-base
    - aci.awired.net/aci-arch-minimal