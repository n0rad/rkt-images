name: awired.net/aci-arch-bind

builder:
  dependencies:
    - awired.net/aci-arch-pacman

aci:
  dependencies:
    - awired.net/aci-base
  app:
    exec: [/usr/sbin/named, -g, '-4', -c, /etc/named.conf, -u, named]
    mountPoints:
      - {name: data, path: /var/bind, readOnly: false}
